{
  "id": "external_visual_handoff_alignment",
  "title": "Structured handoff messages when external tools fail",
  "status": "READY_FOR_NEXT_SLICE",
  "category": "runtime_stability",
  "priority": "P0",
  "description": "When an external tool fails (login, captcha, permission, quota, timeout, DOM error, offscreen/minimized window, black capture), the UI shows a structured message: WHAT tool, WHAT block, WHAT evidence, WHAT human action, whether local fallback is viable.",
  "resume_hint": "P0.3 visual alignment slice implemented: offscreen/minimized window detection, black capture rejection, guided visual handoff, runtime audit trace for invalid evidence. Awaiting live Windows proof.",
  "next_action": "P0.3 visual alignment — user runs IABV, triggers ChatGPT consultation with minimized window, verifies guided handoff message appears instead of 'captura lista'. Then mark COMPLETED.",
  "metadata": {
    "covered_by_pr": "#377",
    "coverage_detail": "_human_external_consultation_failure classifies 7 failure types (security_verification, codex_state_missing, access_denied, session_expired, timeout, quota, generic). Each produces structured handoff with tool name, block type, evidence, human action, and local fallback mention.",
    "p01_additions": "Runtime audit trace logs blocked external tools with terminal_state and provider info. Blocked dispatches correctly derive terminal_state from payload.meta (not 'success').",
    "p02_additions": "trace_dispatch_started() emits provider and user_goal_excerpt on external_consultation spawn. Lifecycle can now be reconstructed: started(provider=ChatGPT) → terminal(blocked_by_permission). latestDispatchLifecycle exposes last result to UI.",
    "p03_additions": "_window_rect_is_captureable() detects offscreen/minimized windows (left/top <= -30000, width/height < 50px). _capture_is_low_information() detects blank captures (blank_probability >= 0.90, dynamic_range == 0, unique_color_count <= 2). _validate_visual_evidence_result() prevents treating black capture as success. _visual_handoff_message() builds structured user guidance. Runtime audit traces visual_evidence_snapshot with status=unresolved. Live evidence: user reported ChatGPT window at rect=[-32000,-32000,199,34], blank_probability=0.98, dynamic_range=0, unique_color_count=1.",
    "p03_live_evidence": {
      "event": "external_assistant_verification_window_opened",
      "target_window": "ChatGPT - Google Chrome for Testing",
      "hwnd": 16319628,
      "rect": [-32000, -32000, 199, 34],
      "capture_scope": "external_target_window_bbox",
      "blank_probability": 0.98,
      "dynamic_range": 0,
      "unique_color_count": 1,
      "useful": false,
      "semantic_state": "screen_visible_but_low_information",
      "unresolved": "UNRESOLVED:visual_capture_low_information"
    },
    "remaining": [
      "Live Windows proof with P0.3 code merged",
      "Deep visual verification (screenshot comparison) not implemented"
    ],
    "unresolved": "Live Windows proof pending — user must run IABV with P0.3 merged and trigger external consultation with offscreen window"
  }
}
